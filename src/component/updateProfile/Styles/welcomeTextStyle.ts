import {StyleSheet, Dimensions} from 'react-native';

const styles = StyleSheet.create({
  welcomeTitle: {
    fontSize: 32,
    fontWeight: '600',
    textAlign: 'center',
    marginBottom: 10,
    color: '#1A1A1A',
  },
  welcomeSubtitle: {
    fontSize: 16,
    textAlign: 'center',
    color: '#6D6D6D',
    marginBottom: 40,
  },
});

export default styles;
